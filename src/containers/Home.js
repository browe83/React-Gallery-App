import React from "react";

//Component renders welcome page, which is rendered at the root.
const Home = (props) => (
  <div className="photo-container">
    <h2>Welcome!</h2>
  </div>
);

export default Home;
